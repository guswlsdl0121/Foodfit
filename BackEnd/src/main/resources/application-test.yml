spring:
  config:
    activate:
      on-profile: test
  jpa:
    hinernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never